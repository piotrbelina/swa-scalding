package piotr

/**
 * Created by bela on 27.03.14.
 */
class Paths (base: String) {
  val tmp = base + "/tmp"
  val newData = base + "/newData"
  val masterData = base + "/masterData"
  val snapshot = base + "/snapshot"
  val shredded = base + "/shredded"
}
